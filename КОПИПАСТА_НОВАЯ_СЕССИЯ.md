# 📋 КОПИПАСТА В НОВУЮ СЕССИЮ — Muslimoon Bot (состояние v93, 07.06.2026)

> Вставь это в начало новой сессии. Отвечать по-русски, показывать % прогресса. Push разрешён («Запушь сам»).
> Владелец тестит вживую и шлёт правки ПАЧКАМИ — работать пакетами, деплоить часто, бэкап держать свежим.

## ПЕРВЫМ ДЕЛОМ
1. Открой превью: `preview_start name=miniapp` (порт 5599, из `.claude/launch.json`) → `#dev`. Скриншот иногда таймаутит (баг харнеса) — проверяй через preview_eval/snapshot, но screenshot иногда РАБОТАЕТ (полезен!).
2. Прочитай в `C:\Users\anzor\Documents\MUSLIMOON BOT claud`: **JOURNAL.md** (записи #056–070), **НАПРАВЛЕНИЯ_06-06.md** (источники+директивы), ЗАЯВКИ.md, ЗАМЕЧАНИЯ.md, CLAUDE.md, этот файл.
3. Память: `~/.claude/.../memory/` (g9-server-access, takhrij-subsystem, deploy-commit-bash-heredoc, miniapp-plan).

## ГДЕ ЧТО (актуально)
- Mini App LIVE: **https://germanyalfurqan-eng.github.io/hadith-bot/** — сейчас **v93**. Исходник `miniapp/index.html` (~2000 строк). Версия в 2 местах (`const APPVER` + `verBadge`).
- Бэкенд `bot.py` на Railway. BACKEND_URL=`https://melodious-acceptance-production-1c9a.up.railway.app/api`. OWNER_ID=131827895. Бот @muslimoontt_bot.
- **ДЕПЛОЙ — из КЛОНА `%TEMP%\hadith-bot-push\`**: правлю `miniapp/index.html`→cp в `КЛОН/docs/index.html`; правлю `bot.py` (обе копии); подними версию; `update_note.txt`; commit (`-c user.name=muslimoon-bot -c user.email=noreply@anthropic.com`, +Co-Authored-By: Claude Opus 4.8); push. После bot.py — тесты `test_g9.py`(45)+`test_g9_api.py`(18) в клоне. ⚠️ Коммит-сообщения — через Bash heredoc (`git commit -F -`), НЕ PowerShell `@'...'@`.
- Бэкап: Google Drive `Documents\Google drive s\Muslimoon_BACKUP\LATEST\` (обновлять при деплое) + `Muslimoon_RECOVERY.zip` (~37МБ, пересобирать из LATEST через temp+copy).
- ⚠️ Edit иногда говорит «file not read» — тогда Read участок и повтори. Grep показывает `/` как `\` в выводе — проверяй реальные байты через `sed -n 'Np' file | cat -A`.

## ✅ ЧТО УЖЕ СДЕЛАНО (v65→v92, главное)
- ⭐ **МУСЛИМ — нумерация Абд аль-Баки** (sunnah.com): `docs/muslim_baqi.json` (2976 хадисов, парсинг локального `sborniks/صحيح مسلم - ت عبد الباقي/`). 2363=пальмы, 2408=ахль бейт (4 риваята, разделитель ⚬). `loadCollection('muslim')`/`getOne('muslim')` грузят оттуда; перевод через поле `fw`→rus-muslim. **ЭТА ЖЕ ТЕХНИКА — для локального Табари.**
- Тафсир (отдельное окно, 6 тафсиров Ибн Касир/Табари/Саади/Мухтасар/Куртуби/Багави, оригинал+перевод, поиск внутри). Тахридж (sunnah.one+все сборники+кэш data/takhrij). Перевод-3-состояния+накопление. Передатчики (подсветка в тексте + ИИ-биография /api/narrator_ai). Помощник аятов (4238→42:38). Тема, недавние, популярное, статистика, зум-выкл, помощник, SEO.

## 🔴 ОЧЕРЕДЬ — ПУНКТЫ ВЛАДЕЛЬЦА (из последнего потока, ПРИОРИТЕТ сверху)
1. **ИИ-ПОИСК реально помогает (M116) — владелец недоволен, «отстаёшь»:** ① транскрипция ПЕРВЫМ делом предлагать схожее арабское (иджтихад→اجتهاد, а не النار); промт v85 чинит, но качество слабое — проработать. ② живые подсказки транскрипции под полем («ляхуа хадис»→لهو الحديث). ③ кнопка «🔄 заново» уже есть.
2. **Выделение отдельного СЛОВА → его смысл/корень** (для чайников): тапнул слово → перевод/корень/где ещё (как corpus.quran.com). Связано с базой корней (M20).
3. **M97: цвета иснад/матн/слова рави/комментарий учёного.** ВАЖНО: когда НЕ уверен — НЕ выделять (сейчас иснад выделяется неправильно) + **кнопка ИИ-разметки** иснад/матн → накопление → серая пометка «сделано ИИ». Принцип: ВЕЗДЕ где ИИ — отметка.
4. **БОТ Муслим 2408 ≠ апп** — бот (Telegram) на fawazahmed0, апп на Абд аль-Баки. **Единый подход**: подключить `muslim_baqi.json` и в bot.py.
5. **Отзывы (feedback): всегда читать ошибки** из журнала + **статусы в истории**: «исправили» / «необоснованно» / «увидели, ожидайте» / «пока не увидели». Система статусов отзыва.
6. **Ботяра (ИИ-чат бота) льёт ВОДУ** — промт: отвечать ЛАКОНИЧНО, по существу, с доводом (не простыни на 2 экрана).
7. **Недавние/Популярное — РАЗНЫЕ для Аятов и Хадисов** (сейчас общие).
8. **236 → предлагать 2:36 И 23:6** (помощник; ayahCandidates вроде уже даёт оба — проверить).
9. **Инструкции «Как искать» — в маленькую кнопку, текст под поиском облегчить.**
10. **Защита нейронки — временной лимит** (rate_ok есть, ужесточить + фронт-кулдаун).
11. **Карточка равия КАК В МАКТАБЕ:** клик по имени → ЦЕЛЫЕ источники الجرح والتعديل (вкладки الملخص/الجرح والتعديل/روى عن/عنه/المرويات). Источники = **9 turath-ссылок владельца** (в НАПРАВЛЕНИЯ_06-06.md): app.turath.io/book/2170,13041,1350,6046,1485,1076,11236,6013,13063.
12. **Локальный Табари** (`sborniks/تفسير الطبري.../`, htm) через Оллама: распарсить по аятам, КАЖДЫЙ хадис/асар отдельно (номера есть!) + под каждым кнопки Перевод/Тахридж-ИИ/Огласовки → полный поиск по тафсиру.
13. ✅ Риваяты Муслима (⚬) — показываются раздельными блоками (v93, cardHadith). Осталось: Функции Мактабы в расширенный поиск. EN/AR. Минимализм-режим. Аудио Корана (Худайфи). Донаты.

## ПРИНЦИПЫ ВЛАДЕЛЬЦА
Накопительный эффект (ИИ раз→дальше из базы) · безопасность всегда · скорость · ТОЧНОСТЬ (не лить неверные данные — лучше честно сказать) · ВЕЗДЕ где ИИ — пометка · достоверность только после НАШЕЙ проверки.
