v1114 — убран мусор «&nbsp;» и другие HTML-коды из текста хадисов
• Вы прислали хадис Ахмада №7120: «тут че творится то» — в тексте виднелся код «&nbsp;» вместо пробела.
• Причина: HTML-мнемоники из сырого издания не раскрывались. Теперь &nbsp; → пробел, &lt;/&gt; → </>, &amp; → &, числовые (&#NNNN;) → символ. Текст чистый.
• Проверено на реальном хадисе №7120: код «&nbsp;» больше не показывается.