# Railway/nixpacks: ставим ffmpeg для аудио-команд бота (mp3 / улучшить / метаданные).
# C32: ffmpeg через nixPkgs (из nix-стора) — надёжнее aptPkgs, которое стало флакать на сборке Railway (билд падал на «Build image»).
[phases.setup]
nixPkgs = ["ffmpeg"]
