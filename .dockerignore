# C32: минимальный контекст сборки — образу нужен только bot.py + requirements.txt (данные бот тянет удалённо).
# Игнорируем ВСЁ, кроме нужного → быстрый билд, лёгкий образ, без 965МБ docs/.
*
!bot.py
!requirements.txt
!Dockerfile
