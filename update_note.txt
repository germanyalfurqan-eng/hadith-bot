🚨 v936 — #492 «почему повторения, ид одинаковые»: подсказки передатчика показывали ОДНОГО И ТОГО ЖЕ равия по нескольку раз (разные текст-формы имени в базе давали отдельные строки с одинаковым id). Добавлена дедупликация по id сразу после сортировки — теперь каждый равий один раз.
